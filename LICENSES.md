# License boundaries

The MIT license in `LICENSE` covers original software in `src/`, `scripts/`,
`tests/` (excluding data fixtures), workflow configuration, original documentation,
and generated HTML/CSS/JavaScript derived from that software.

It does **not** cover `public/data/`, `tests/fixtures/`, third-party quotations or
source material in `src/events.js` and `src/signal-context.json`, or any trademarks.
Original selection and annotation code remains MIT; third-party content does not.
See [data terms](public/data/LICENSE.txt). Generated HTML can contain both original
code and attributed third-party data/context; bundling does not erase that boundary.
